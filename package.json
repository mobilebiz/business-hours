{
  "name": "business-hours",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "npm version patch --force && twilio serverless:deploy --force"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "12"
  }
}
